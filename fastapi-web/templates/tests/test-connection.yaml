apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-web.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
